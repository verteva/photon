export interface FiguresData {
  calculation: number;
  calcInterval: number;
  min: number;
  max: number;
}

export const CardStyles = {
  PRIMARY: 'primary',
  SECONDARY: 'secondary',
  TERTIARY: 'tertiary',
} as const;
